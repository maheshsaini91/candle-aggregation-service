# Default DB credentials — must match application.properties (datasource section).
# Used by docker-compose for PostgreSQL and by the app when using profile=db.
POSTGRES_DB=aggregation
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
